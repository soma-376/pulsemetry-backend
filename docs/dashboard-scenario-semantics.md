# PROJ-156 시나리오 관측 의미 검토

참고 개요 §6의 46개 시나리오와 현재 실행·판정 코드를 대조했다. 42개 실행 가능 경로 중 전후 비교 5개는 [별도 근거](dashboard-comparison-acceptance.md)에 있으며, 아래는 나머지 37개다. available/partial은 카탈로그의 실행 가용성으로, 제목이 암시하는 인과관계·위험 탐지·경제 효과가 입증됐다는 뜻이 아니다.

## 실행 가능한 나머지 37개

| ID | 실제 관측·판정 | 해석 한계 | 대표 DB 테스트 |
|---|---|---|---|
| S1-1 | 팀 USD/백만 토큰 예산 초과 | 입력 기간 예산이며 청구액·배분 적정성이 아님 | 예산 시나리오는 USD와 백만 토큰을 각 팀의 관측 합계와 비교한다 |
| S1-2 | 전체 이름·대소문자·* 패턴의 모델 비용 | 티어·작업 난이도·대체 비용을 검증하지 않음 | 프리미엄 패턴은 전체 이름 대소문자와 별표만 해석하고 모델 비용과 토큰을 선택한다 |
| S1-3 | 이동평균 대비 비용 임계값, 재시도 5% 이상, 모델 비중 30%p 이상 증가 | 재시도 추가 비용이나 폭주 원인을 확정하지 않음; 비중은 충분한 공개 시계열에서만 비교 | 실제 비용 시나리오는 큐 워커 결과 조회까지 연결된다 |
| S1-4 | 유효 토큰 요청의 캐시 읽기 비율 0 | 프롬프트 유사도·캐싱 가능성·절감 효과 미측정 | 캐싱 시나리오는 토큰과 프롬프트 분포를 조회하고 캐시 영을 안내한다 |
| S1-5 | 입력/출력 비율 임계값 초과 | 컨텍스트 첨부 여부 미관측 | 컨텍스트 시나리오는 세 지표와 임계값 초과 판정을 실행한다 |
| S1-6 | model/effort 및 model/speed별 delta 비용 | 같은 비용의 별도 분류로 합산 금지; 낭비·절감액 아님 | 모델 effort 시나리오는 delta 비용 차원과 토큰 세션을 제공한다 |
| S1-7 | 기준 시점의 장기 미관측 활성 설치 수 | 현재 상태와 보존 이력 기준; 라이선스 회수 가능성 아님 | 유휴 관측은 과거 기준 이후 이벤트를 제외하고 경계 이상 경과 설치를 집계한다 |
| S2-1 | 현지 요일·시간 히트맵과 제한 이벤트 관측 | 오전/오후 변동 원인·작업 중단 미판정 | 시간대 시나리오는 현지 요일 시간의 168칸과 제한 이벤트를 제공한다 |
| S2-2 | Rate Limit 이벤트 건수 | 상시 도달·실제 한도 소진·재시도 고갈 미판정 | 제한 시나리오는 owner 감사 후 실행하고 권한 변경을 재검증한다 |
| S2-3 | 지정 현지 날짜의 세션 관측 | 스프린트 구간·반복 주기·인과 효과 미추정 | 스프린트 시나리오는 현지 날짜와 세션을 연결하고 시간대별 프롬프트를 제공한다 |
| S3-1 | 팀별 채택률과 보조 지표 | 직군 대신 팀; 직군 격차·팀 우열 미판정 | 채택 시나리오는 팀별 다섯 지표와 관측 채택률을 제공한다 |
| S3-2 | 익명 상위 10% 토큰 점유율·로렌츠 곡선 | 개인 식별·생산성·의존 위험 미판정 | 집중도 시나리오는 익명 요약과 로렌츠 곡선을 제공한다 |
| S3-3 | 선택 첫 관측 코호트의 TTFU·잔존율·활동 | finding 없음; 실제 신규 가입·보존 이전 사용 미확정; 진행 주 미판정 | 온보딩 시나리오는 코호트 밖 첫 사용을 제외하고 동일 설치의 이후 활동을 추적한다 |
| S3-4 | 팀별 세션 관측과 기간 집계 | 팀 규모·수집 범위 보정 없는 활용 우열 미판정 | 팀 활용 시나리오는 네 지표의 팀별 기간 합계를 제공한다 |
| S3-5 | 서브에이전트 비용 비율 관측 | 스킬·플러그인 활용률·숙련도 미측정 | 고급 기능 시나리오는 서브에이전트 비용과 보조 지표를 제공한다 |
| S4-1 | 세션별 프롬프트 p50>1 | 원문 반복·재시도와 동일하지 않음 | 프롬프트 시나리오는 중앙값 초과와 토큰을 함께 제공한다 |
| S4-2 | 관측 산출 없는 세션 비율>0 | 세션 종료·실제 대화 포기를 확정하지 않음 | 대화 시나리오는 산출 없는 세션 비율과 마지막 이벤트를 연결한다 |
| S4-3 | 선택 언어의 편집 수락률 관측 | 언어는 수락률에만 적용; revert·품질·생산성 미측정 | 코드 수용 시나리오는 선택 언어의 비율과 전체 기간 보조 지표를 구분한다 |
| S4-5 | 선택 명령 프롬프트/전체 프롬프트 비율 | 스킬·템플릿 내용·생산성 미측정 | 템플릿 시나리오는 선택 명령만 분자에 포함하고 전체 프롬프트 분모를 유지한다 |
| S4-6 | action별 도구 호출 | 업무 주제·유즈케이스 분류 아님 | 유즈케이스 시나리오는 action별 기간 합계를 분리한다 |
| S4-8 | 팀별 승인 대기 p90의 분 임계값 초과 | Plan 전용 대기·생산성 손실 아님; 보조 지표는 선택 범위 전체 | 대기 시나리오는 팀별 임계값을 판정하고 다른 팀의 인원으로 소집단을 노출하지 않는다 |
| S5-2 | MCP 연결 관측·읽기 밀도 | 전송 내용·목적지·승인을 확인하지 않아 유출 탐지 아님 | 외부 접근 시나리오는 감사 후 MCP와 읽기 밀도를 제공한다 |
| S5-4 | 일별 최신 비어 있지 않은 벤더 계정과 등록 계정 불일치 수 | 주소 없는 관측 요약; 비인가·개인 계정·섀도우 AI 확정 아님 | 섀도우 관측은 감사 사유를 보존하고 주소 없는 일별 불일치를 반환한다 |
| S5-5 | UTC epoch 정렬 고정 창에서 거부 건수≥probe_count | 이동 창·개인별 반복·공격 여부 아님; 창마다 거부 구성원 5명 이상 | 반복 거부 창은 조회 경계와 고정 창 및 최소 횟수를 적용한다 |
| S5-7 | 유효 사용자 accept 중 threshold_ms 미만 비율 | 검증 여부·반출·PR 인과관계 미확인 | 즉시 승인 시나리오는 지정 임계값과 엄격 경계를 적용하고 감사한다 |
| S6-1 | 선택 모델의 거부 응답 관측 | P2라도 owner 감사; 설문·품질 점수·만족도 아님 | 품질 피드백 시나리오는 P2에서도 owner 감사와 모델 범위를 적용한다 |
| S6-4 | 선택 모델의 첫 토큰 지연 p90>0 | 별도 SLA·장애 임계값·원인 분석 아님 | 레이턴시 시나리오는 모델 범위를 모든 지표와 결과 필터에 적용한다 |
| S6-5 | 전체 관측 호출 중 attempt>1 비율 | attempt 미수집도 분모 포함; 스톰·고갈·추가 비용 아님 | 재시도 시나리오는 감사 후 재시도 비율과 전체 비용을 제공한다 |
| S7-1 | 도구 실패율 임계값 초과 | 에이전트 태스크 완료·성공률 직접 측정 아님 | 에이전트 시나리오는 실패율 임계값과 네 지표를 실행한다 |
| S7-2 | 세션별 read/search/fetch 호출 p90 임계값 초과 | 접근 권한·데이터 내용·외부 전송 미확인 | 읽기 밀도 시나리오는 세션 p90과 지정 임계값을 엄격 비교한다 |
| S7-3 | 도구 거절 관측과 차단·대기 보조 지표 | 위반·악의적 사용·에스컬레이션 미확정 | 정책 시나리오는 감사 후 거절 차단 대기를 제공한다 |
| S7-4 | 현재 활성 설치 대비 관측 커버리지 | 인증 감사·삭제 실행 데이터 없음; 감사 완전성·보존 정책 준수 아님 | 감사 거버넌스 시나리오는 활성 설치 대비 관측 커버리지를 제공한다 |
| S8-1 | 세션 사용 관측과 일곱 보고 지표 | 재무 연결·ROI·절감액·생산성 미계산 | 경영 보고 시나리오는 일곱 지표와 사용자당 비용을 제공한다 |
| S8-2 | 완결된 일별 비용의 constant/linear 30일 외삽 | 미관측은 0이 아님; 이력 부족 시 insufficient_data; 청구액·신뢰구간 없음 | 비용 예측은 90일 실측의 일정 평균과 선형 추세를 구분한다 |
| S8-3 | 같은 기간 두 모델의 독립 집계 차이 | 무작위 배정·업무·요청량 통제 없음; 모델 우열·유의성 아님 | 모델 비교는 동일 기간에 두 모델을 독립 집계하고 모델 라벨을 보존한다 |
| S8-4 | 제품별 비용과 제품/모델 토큰 | 모델 공급자·계약·전환 비용 확인 없이 벤더 종속 판정 불가 | 벤더 시나리오는 제품 모델 토큰과 제품 비용을 분리한다 |
| S8-5 | 제품별 활성 사용자·사용자당 비용 | 사용자 중복 가능; 통합 절감액 미계산; 도구 호출은 전체 범위 | 도구 통합 시나리오는 제품별 사용자와 사용자당 비용을 분리한다 |

## 소스와 검증 범위

- 실행 분기는 scenario/DashboardScenarioRuns.kt, 입력·기간·권한 범위는 scenario/DashboardScenarioInput.kt, 20개 관측 임계 규칙은 finding/DashboardThresholdFindings.kt에 있다. 나머지는 같은 finding/ 아래 DashboardBudgetFindings, DashboardPremiumModels, DashboardSpikeFindings, DashboardInactivity, DashboardSprintFindings, DashboardAdoptionFindings, DashboardConcentrationFindings, DashboardTeamUsageFindings, DashboardActionFindings, DashboardGateFindings, DashboardProbeWindows, DashboardCostForecast, DashboardModelComparison, DashboardVendorFindings, DashboardProductUsageFindings, DashboardModelEffortFindings에 분리되어 있다. 온보딩은 전용 실행에서 프레임을 반환하고 findings는 빈 목록이다.
- 대표 테스트는 모두 apps/dashboard-api/src/test/kotlin/com/team376/pulsemetry/dashboard/DashboardAuthTest.kt에 있다. 표의 37개 이름을 실제 소스와 최신 JUnit XML에 대조해 통과를 확인했다. 대표 테스트 하나가 가능한 모든 입력 조합을 검증한다는 뜻은 아니다.
- 최신 런타임 86a2a29의 dashboard 테스트 266건과 실제 frontend E2E가 통과했다. E2E의 42개 실행 성공은 모든 시나리오의 양수 판정이나 제목의 의미를 입증하지 않는다. 권한·감사·실행 및 저장 수명주기 검증은 dashboard-acceptance-matrix.md에서 추적한다.
- S8-2의 30일 예측은 현재 구현 기본값이며 사용자가 별도로 확정한 예측 기간이 아니다. S1-2의 전체 이름 매칭/* 와일드카드와 S6-3/S8-7의 전후 4주는 사용자 확정 사항이다.

## 실행 불가 4개

| ID | 현재 제한 | API 동작 |
|---|---|---|
| S4-7 | 외부 DORA/성과 데이터 없음 | scenario_unavailable, 409 |
| S5-1 | 민감정보 판정에 필요한 원문 미취급 | scenario_unavailable, 409 |
| S5-3 | 고객 데이터 판정에 필요한 원문 미취급 | scenario_unavailable, 409 |
| S6-2 | 환각 신고 외부 채널 데이터 없음 | scenario_unavailable, 409 |

## 검토 결론과 남은 결정

46개 ID의 실행 가능/불가 분류와 42개 관측 결과의 의미 대조 목록을 완성했다. 직전 S8-5 제품별 비용과 S4-8 팀별 대기 보완 외에, 이번 검토에서는 추가 런타임 수정 근거를 발견하지 못했다. 이는 관측 API의 검토 완료이며 제품 전체 수용 완료가 아니다.

남은 수용 작업은 일반 UI의 감사 사유 전달(CSV·P3 시작·주소 표), 주소/비교/잔존율의 실제 화면 표시, 지표별 미지원 distribution의 표본·버킷 정의다. 제목 수준의 공격 탐지·인과 효과·성과 분석을 추가하려면 별도의 데이터와 판정 요구가 필요하다. 명세에 없는 임계값이나 인과 결론을 만들어 현재 완료 범위를 넓히지 않는다. 운영 장애 추가 범위는 dashboard-recovery-verification.md의 검증 범위와 구분한다.
